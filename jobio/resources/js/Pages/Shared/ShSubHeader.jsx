const ShSubHeader = ({ children }) => {
    return (
        <h6 className="flex-[0_0_100%] text-sm font-[400] text-gray-400">
            {children}
        </h6>
    );
};

export default ShSubHeader;
